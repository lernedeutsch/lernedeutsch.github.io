<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>Sprich nach — A1 · Lektion 1</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Kalam:wght@400;700&family=Nunito+Sans:opsz,wght@6..12,400,600,700,800&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">

<style>
:root{
  --bg:#173047;
  --bg2:#203a57;
  --surface:#f8fbff;
  --card:#ffffff;
  --text:#101820;
  --muted:#41556b;
  --line:#d9e4f0;
  --accent:#f4c95d;
  --accent-2:#6ac6ff;
  --accent-3:#84df42;
  --accent-4:#a88af5;
  --danger:#ef6a5f;
  --shadow:0 14px 35px rgba(10,18,32,.16);
  --shadow-2:0 10px 22px rgba(10,18,32,.10);
}

*{box-sizing:border-box}
html{scroll-behavior:smooth}

body{
  margin:0;
  color:var(--text);
  font:400 1rem/1.65 "Nunito Sans",sans-serif;
  background:
    radial-gradient(circle at top left,rgba(255,255,255,.08),transparent 30%),
    radial-gradient(circle at bottom right,rgba(255,255,255,.06),transparent 34%),
    linear-gradient(180deg,var(--bg),var(--bg2));
  min-height:100vh;
}

button,input{font:inherit}

.shell{
  max-width:1150px;
  margin:0 auto;
  padding:20px 16px 36px;
}

.layout{
  display:flex;
  align-items:flex-start;
  gap:14px;
}

.layout .panel{
  flex:1;
  min-width:0;
}

.top-eyebrow{
  text-align:center;
  color:#dcecff;
  font:700 .74rem "JetBrains Mono",monospace;
  letter-spacing:.18em;
  text-transform:uppercase;
  margin:0 0 6px;
}

.top-title{
  margin:0 0 18px;
  text-align:center;
  color:#fff;
  font:700 clamp(1.3rem,3vw,1.85rem)/1.15 "Kalam",cursive;
}

/* Kleines, schmales Fenster mit allen richtig gesprochenen Sätzen — jetzt links */
.history-panel{
  display:flex;
  flex-direction:column;
  width:190px;
  flex-shrink:0;
  max-height:min(640px,78vh);
  background:linear-gradient(180deg,#1c3450,#132538);
  border:1px solid rgba(255,255,255,.12);
  border-radius:22px;
  box-shadow:var(--shadow);
  padding:14px 12px;
  overflow:hidden;
}

.history-title{
  margin:0 0 4px;
  color:#fff;
  font:700 .95rem "Kalam",cursive;
  text-align:center;
}

.history-sub{
  margin:0 0 10px;
  color:#a9c2dc;
  font:700 .68rem "JetBrains Mono",monospace;
  letter-spacing:.1em;
  text-transform:uppercase;
  text-align:center;
}

.history-list{
  flex:1;
  overflow-y:auto;
  display:flex;
  flex-direction:column;
  gap:7px;
  padding-right:2px;
}

.history-empty{
  color:#7d93ac;
  font-size:.84rem;
  text-align:center;
  margin-top:18px;
}

.history-item{
  padding:8px 9px;
  background:rgba(255,255,255,.94);
  border-radius:11px;
  font-size:.82rem;
  line-height:1.35;
  animation:historyIn .3s ease;
}

.history-item .h-num{
  color:#3d7d1a;
  font:800 .7rem "JetBrains Mono",monospace;
  margin-right:4px;
}

@keyframes historyIn{
  from{opacity:0;transform:translateX(8px)}
  to{opacity:1;transform:none}
}

@media(max-width:820px){
  .layout{flex-direction:column}
  .history-panel{width:100%;max-height:200px}
}

.panel{
  background:linear-gradient(180deg,#fff8eb 0%,#fff 16%,#fff 100%);
  border-radius:22px;
  box-shadow:var(--shadow);
  padding:clamp(16px,3vw,30px);
}

/* Progress */
.progress-row{
  display:flex;
  align-items:center;
  gap:10px;
  margin-bottom:18px;
}

.progress-track{
  flex:1;
  height:7px;
  background:#eef4fb;
  border:1px solid var(--line);
  border-radius:999px;
  overflow:hidden;
}

.progress-fill{
  height:100%;
  width:0%;
  background:linear-gradient(90deg,var(--accent-2),var(--accent-3));
  border-radius:999px;
  transition:width .45s ease;
}

.progress-count{
  color:var(--muted);
  font:700 .8rem "JetBrains Mono",monospace;
  white-space:nowrap;
}

/* Orb */
.stage{
  display:grid;
  place-items:center;
  padding:8px 0 4px;
}

.orb-wrap{
  position:relative;
  width:150px;
  height:150px;
  display:grid;
  place-items:center;
}

.orb-ring{
  position:absolute;
  inset:0;
  border-radius:50%;
  border:2px solid rgba(32,50,74,.10);
  opacity:0;
}

.orb{
  position:relative;
  width:100px;
  height:100px;
  border-radius:50%;
  display:grid;
  place-items:center;
  background:linear-gradient(155deg,#26405e,#152738);
  box-shadow:0 14px 26px rgba(10,20,35,.28), inset 0 0 0 5px rgba(255,255,255,.05);
  transition:background .4s ease,box-shadow .4s ease;
  font-size:34px;
}

.orb-icon{transition:transform .25s ease}

/* states */
.stage[data-state="idle"] .orb{background:linear-gradient(155deg,#2c4a6b,#16283b)}

.stage[data-state="speaking"] .orb{
  background:linear-gradient(155deg,#3da8f0,#1c6fb0);
  box-shadow:0 14px 28px rgba(61,168,240,.4), inset 0 0 0 5px rgba(255,255,255,.08);
}
.stage[data-state="speaking"] .orb-ring{
  border-color:rgba(61,168,240,.55);
  animation:ringPulse 1.6s ease-out infinite;
}

.stage[data-state="listening"] .orb{
  background:linear-gradient(155deg,#8be24a,#4f9b1f);
  box-shadow:0 14px 28px rgba(139,226,74,.4), inset 0 0 0 5px rgba(255,255,255,.08);
}
.stage[data-state="listening"] .orb-icon{animation:micBeat 1s ease-in-out infinite}
.stage[data-state="listening"] .orb-ring{
  border-color:rgba(139,226,74,.55);
  animation:ringPulse 1.1s ease-out infinite;
}

.stage[data-state="correct"] .orb{
  background:linear-gradient(155deg,#84df42,#4f9b1f);
  box-shadow:0 14px 28px rgba(132,223,66,.45), inset 0 0 0 5px rgba(255,255,255,.1);
}

.stage[data-state="incorrect"] .orb{
  background:linear-gradient(155deg,#f28b7f,#c94a3c);
  box-shadow:0 14px 28px rgba(239,106,95,.4), inset 0 0 0 5px rgba(255,255,255,.08);
  animation:shake .4s ease;
}

.stage[data-state="done"] .orb{
  background:linear-gradient(155deg,#f0bd3a,#b98200);
  box-shadow:0 14px 28px rgba(240,189,58,.45), inset 0 0 0 5px rgba(255,255,255,.1);
}

@keyframes ringPulse{
  0%{transform:scale(.86);opacity:.7}
  100%{transform:scale(1.32);opacity:0}
}
@keyframes micBeat{
  0%,100%{transform:scale(1)}
  50%{transform:scale(1.14)}
}
@keyframes shake{
  0%,100%{transform:translateX(0)}
  25%{transform:translateX(-6px)}
  75%{transform:translateX(6px)}
}

.status-line{
  margin:12px 0 4px;
  text-align:center;
  color:var(--muted);
  font:700 .92rem "Nunito Sans",sans-serif;
  min-height:1.4em;
}

/* Sentence display */
.sentence-box{
  margin:10px auto 6px;
  max-width:60ch;
  min-height:62px;
  padding:14px 18px;
  background:#f7fbff;
  border:1px solid var(--line);
  border-radius:18px;
  text-align:center;
}

.sentence-text{
  font:700 clamp(1rem,2.2vw,1.3rem)/1.4 "Kalam",cursive;
  color:var(--text);
}

.sentence-text .word{
  display:inline-block;
  opacity:0;
  transform:translateY(6px);
  transition:opacity .3s ease,transform .3s ease;
}

.sentence-text .word.show{
  opacity:1;
  transform:none;
}

.you-said{
  margin:6px auto 0;
  max-width:60ch;
  min-height:22px;
  text-align:center;
  color:#8c6700;
  font:600 .95rem "Nunito Sans",sans-serif;
}

.you-said.ok{color:#3d7d1a}
.you-said.bad{color:#b23c30}

/* Start overlay */
.start-wrap{
  display:grid;
  place-items:center;
  padding:16px 0 6px;
  text-align:center;
}

.start-lead{
  max-width:44ch;
  margin:6px auto 18px;
  color:var(--muted);
}

.start-btn{
  display:inline-flex;
  align-items:center;
  gap:10px;
  padding:14px 30px;
  color:#fff;
  background:linear-gradient(180deg,#6ac6ff,#3da8f0);
  border:none;
  border-radius:999px;
  cursor:pointer;
  font:800 1.05rem "Nunito Sans",sans-serif;
  box-shadow:0 12px 22px rgba(61,168,240,.35);
  transition:transform .18s,filter .18s;
}
.start-btn:hover{transform:translateY(-3px);filter:brightness(1.05)}

/* Controls */
.controls{
  margin-top:16px;
  display:none;
  flex-direction:column;
  gap:12px;
}

.controls.active{display:flex}

.speed-row{
  display:flex;
  flex-wrap:wrap;
  align-items:center;
  gap:10px;
  padding:11px 13px;
  background:#fff;
  border:1px solid var(--line);
  border-radius:14px;
}

.speed-row label{
  font-weight:700;
  color:var(--text);
  white-space:nowrap;
  font-size:.9rem;
}

.speed-row input[type="range"]{
  flex:1;
  min-width:80px;
  accent-color:#3da8f0;
}

.speed-val{
  min-width:34px;
  text-align:right;
  color:var(--muted);
  font:700 .8rem "JetBrains Mono",monospace;
}

.speed-row .pill-btn{
  flex:0 0 auto;
  padding:9px 14px;
  font-size:.85rem;
}

.btn-row{
  display:flex;
  flex-wrap:wrap;
  gap:9px;
}

.pill-btn{
  flex:1 1 150px;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:7px;
  padding:10px 14px;
  border:1px solid var(--line);
  border-radius:14px;
  background:#fff;
  color:var(--text);
  cursor:pointer;
  font:700 .88rem "Nunito Sans",sans-serif;
  transition:transform .16s,background .16s,border-color .16s,box-shadow .16s;
}

.pill-btn:hover{
  transform:translateY(-2px);
  background:#f3f7fc;
  border-color:#a9cdf2;
  box-shadow:0 8px 16px rgba(0,0,0,.06);
}

.pill-btn.warn{color:#a03a2c;border-color:#f2c8c1}
.pill-btn.warn:hover{background:#fff3f1;border-color:#f0a99c}

.text-fallback{
  display:flex;
  gap:10px;
}

.text-fallback input{
  flex:1;
  padding:11px 13px;
  border:1px solid var(--line);
  border-radius:14px;
  background:#fff;
}

.text-fallback input:focus{
  outline:3px solid #cfe8ff;
  border-color:#6ac6ff;
}

.send-btn{
  padding:11px 16px;
  border:none;
  border-radius:14px;
  background:linear-gradient(180deg,#94e44f,#5fbd28);
  color:#fff;
  font-weight:800;
  cursor:pointer;
  transition:transform .16s,filter .16s;
}
.send-btn:hover{transform:translateY(-2px);filter:brightness(1.05)}

.no-mic-note{
  font-size:.84rem;
  color:var(--muted);
  text-align:center;
}

/* Done screen */
.done-wrap{
  display:none;
  text-align:center;
  padding:10px 0;
}
.done-wrap.active{display:block}

.done-emoji{font-size:46px;margin-bottom:8px}
.done-title{font:700 1.4rem "Kalam",cursive;margin:0 0 6px}
.done-text{color:var(--muted);margin:0 0 16px}

/* Footer nav */
.footer-nav{
  display:flex;
  justify-content:center;
  gap:14px;
  margin-top:18px;
}

.icon-btn{
  display:inline-flex;
  align-items:center;
  gap:8px;
  padding:9px 15px;
  color:var(--text);
  background:#fff;
  border:1px solid var(--line);
  border-radius:999px;
  text-decoration:none;
  cursor:pointer;
  font:700 .88rem "Nunito Sans",sans-serif;
  transition:transform .16s,background .16s,border-color .16s;
}
.icon-btn:hover{
  transform:translateY(-2px);
  background:#f3f7fc;
  border-color:#f2d06f;
}

@media(max-width:560px){
  .panel{border-radius:20px}
  .orb-wrap{width:120px;height:120px}
  .orb{width:86px;height:86px;font-size:28px}
  .btn-row{flex-direction:column}
  .text-fallback{flex-direction:column}
  .speed-row{flex-direction:column;align-items:stretch}
  .speed-row .pill-btn{flex:1 1 auto}
}

@media(prefers-reduced-motion:reduce){
  *,*::before,*::after{
    animation-duration:.01ms!important;
    animation-iteration-count:1!important;
    transition-duration:.01ms!important;
  }
}
</style>
</head>
<body id="oben">

<div class="shell">

  <p class="top-eyebrow">Sprich nach · A1.1</p>
  <h1 class="top-title">Hören und nachsprechen 🎧</h1>

  <div class="layout">

  <div class="history-panel">
    <h2 class="history-title">Deine Sätze</h2>
    <p class="history-sub">Richtig gesprochen</p>
    <div class="history-list" id="history-list">
      <p class="history-empty" id="history-empty">Hier erscheinen deine richtig gesprochenen Sätze.</p>
    </div>
  </div>

  <div class="panel">

    <!-- Start screen -->
    <div class="start-wrap" id="start-wrap">
      <div class="orb-wrap" style="margin-bottom:6px">
        <div class="orb"><span class="orb-icon">👋</span></div>
      </div>
      <p class="start-lead">
        Du hörst einen deutschen Satz aus Lektion 1, sprichst ihn nach
        und siehst sofort, ob es richtig war. 21 Sätze und Dialoge warten.
      </p>
      <button class="start-btn" id="start-btn">▶ Start</button>
    </div>

    <!-- Exercise screen -->
    <div id="exercise-wrap" style="display:none">

      <div class="progress-row">
        <div class="progress-track">
          <div class="progress-fill" id="progress-fill"></div>
        </div>
        <span class="progress-count" id="progress-count">0 / 21</span>
      </div>

      <div class="stage" id="stage" data-state="idle">
        <div class="orb-wrap">
          <div class="orb-ring"></div>
          <div class="orb"><span class="orb-icon" id="orb-icon">🔊</span></div>
        </div>
      </div>

      <p class="status-line" id="status-line">Bereit …</p>

      <div class="sentence-box">
        <p class="sentence-text" id="sentence-text"></p>
      </div>

      <p class="you-said" id="you-said"></p>

      <div class="controls" id="controls">

        <div class="speed-row">
          <label for="speed">🐢 Geschwindigkeit</label>
          <input type="range" id="speed" min="0.6" max="1.3" step="0.05" value="0.85">
          <span class="speed-val" id="speed-val">0.85</span>
          <button class="pill-btn" id="restart-btn">🔄 Neustart</button>
        </div>

        <div class="btn-row">
          <button class="pill-btn" id="repeat-btn">🔁 Letzte Frage wiederholen</button>
          <button class="pill-btn" id="next-btn">⏭ Nächstes</button>
          <button class="pill-btn warn" id="stop-btn">⏹ Stop</button>
          <button class="pill-btn" id="weiter-btn">▶ Weiter</button>
        </div>

        <div class="text-fallback">
          <input type="text" id="text-input" placeholder="Falls du nicht sprechen kannst: hier tippen …" autocomplete="off">
          <button class="send-btn" id="send-btn">Senden</button>
        </div>

        <p class="no-mic-note" id="no-mic-note" style="display:none">
          Dein Browser unterstützt keine Spracherkennung — nutze bitte das Textfeld oben.
        </p>
      </div>

      <!-- Done screen -->
      <div class="done-wrap" id="done-wrap">
        <div class="done-emoji">🏆</div>
        <h2 class="done-title">Geschafft!</h2>
        <p class="done-text">Du hast alle Sätze aus Lektion 1 nachgesprochen. Klasse gemacht!</p>
        <button class="pill-btn" id="done-restart-btn" style="max-width:260px;margin:0 auto">🔄 Noch einmal üben</button>
      </div>

    </div>

  </div>

  </div>

  <nav class="footer-nav">
    <a href="../../A1/1.html#lektion1-teil3" class="icon-btn">← Zurück zur Lektion</a>
    <a href="#oben" class="icon-btn">↑ Nach oben</a>
  </nav>

</div>

<script>
/* ---------- Übungssätze aus Lektion 1 ---------- */
const sentences=[
  "Guten Morgen!",
  "Guten Tag!",
  "Guten Abend!",
  "Hallo!",
  "Tschüss!",
  "Auf Wiedersehen!",
  "Ich heiße Anna.",
  "Mein Name ist Anna Müller.",
  "Ich bin Anna.",
  "Wie heißt du?",
  "Wie heißen Sie?",
  "Hallo!",
  "Wie heißt du?",
  "Ich heiße Lena.",
  "Und du?",
  "Ich heiße Max.",
  "Guten Tag.",
  "Wie heißen Sie?",
  "Ich heiße Thomas.",
  "Und Sie?",
  "Ich heiße Julia."
];

let currentIndex=0;
let currentRate=0.85;
let awaitingRetry=false;
let recognizing=false;
let recognition=null;

const stage=document.getElementById("stage");
const statusLine=document.getElementById("status-line");
const sentenceText=document.getElementById("sentence-text");
const youSaid=document.getElementById("you-said");
const progressFill=document.getElementById("progress-fill");
const progressCount=document.getElementById("progress-count");
const controls=document.getElementById("controls");
const doneWrap=document.getElementById("done-wrap");
const exerciseWrap=document.getElementById("exercise-wrap");
const startWrap=document.getElementById("start-wrap");
const orbIcon=document.getElementById("orb-icon");
const noMicNote=document.getElementById("no-mic-note");
const historyList=document.getElementById("history-list");
const historyEmpty=document.getElementById("history-empty");

/* ---------- Deutsche Frauenstimme auswählen ---------- */
let germanVoice=null;

function loadGermanVoice(){
  if(!("speechSynthesis" in window))return;
  const voices=speechSynthesis.getVoices();
  const femaleHints=["female","frau","anna","petra","helena","vicki","marlene","google"];

  germanVoice=
    voices.find(v=>v.lang?.toLowerCase()==="de-de"&&femaleHints.some(h=>v.name.toLowerCase().includes(h)))||
    voices.find(v=>v.lang?.toLowerCase()==="de-de")||
    voices.find(v=>v.lang?.toLowerCase().startsWith("de"))||
    null;
}

loadGermanVoice();
if("speechSynthesis" in window){
  speechSynthesis.addEventListener("voiceschanged",loadGermanVoice);
}

/* ---------- Satz sprechen + synchron anzeigen ---------- */
function renderSentenceShell(text){
  const words=text.split(" ");
  sentenceText.innerHTML=words.map(w=>`<span class="word">${w}</span>`).join(" ");
}

function revealWordsTimed(text,rate){
  const words=sentenceText.querySelectorAll(".word");
  const wordsPerMs=(150*rate)/60000; // grobe Schätzung: ~150 Wörter/Min bei rate=1
  const stepMs=Math.max(90,1/wordsPerMs);
  words.forEach((w,i)=>{
    setTimeout(()=>w.classList.add("show"),i*stepMs);
  });
}

function speak(text,rate){
  return new Promise(resolve=>{
    if(!("speechSynthesis" in window)){
      renderSentenceShell(text);
      revealWordsTimed(text,rate);
      resolve();
      return;
    }
    speechSynthesis.cancel();
    const utter=new SpeechSynthesisUtterance(text);
    utter.lang="de-DE";
    utter.rate=rate;
    utter.pitch=1.05;
    utter.volume=1;
    if(germanVoice)utter.voice=germanVoice;

    utter.onstart=()=>{
      renderSentenceShell(text);
      revealWordsTimed(text,rate);
    };
    utter.onend=resolve;
    utter.onerror=resolve;
    speechSynthesis.speak(utter);
  });
}

/* ---------- Textvergleich (tolerant) ---------- */
function normalize(str){
  return str
    .toLowerCase()
    .replace(/[.,!?;:„“"']/g,"")
    .replace(/ß/g,"ss")
    .replace(/ä/g,"a").replace(/ö/g,"o").replace(/ü/g,"u")
    .trim()
    .replace(/\s+/g," ");
}

function levenshtein(a,b){
  const m=a.length,n=b.length;
  const dp=Array.from({length:m+1},()=>new Array(n+1).fill(0));
  for(let i=0;i<=m;i++)dp[i][0]=i;
  for(let j=0;j<=n;j++)dp[0][j]=j;
  for(let i=1;i<=m;i++){
    for(let j=1;j<=n;j++){
      dp[i][j]=a[i-1]===b[j-1]
        ?dp[i-1][j-1]
        :1+Math.min(dp[i-1][j-1],dp[i-1][j],dp[i][j-1]);
    }
  }
  return dp[m][n];
}

function isMatch(said,target){
  const a=normalize(said);
  const b=normalize(target);
  if(!a)return false;
  if(a===b)return true;
  const dist=levenshtein(a,b);
  const similarity=1-dist/Math.max(a.length,b.length);
  return similarity>=0.72;
}

/* ---------- Spracherkennung ---------- */
const SpeechRec=window.SpeechRecognition||window.webkitSpeechRecognition;

if(SpeechRec){
  recognition=new SpeechRec();
  recognition.lang="de-DE";
  recognition.interimResults=true;
  recognition.continuous=false;
  recognition.maxAlternatives=1;
}else{
  noMicNote.style.display="block";
}

let recognitionTimeout=null;
let gotFinal=false;

function startListening(){
  if(!recognition)return;
  gotFinal=false;
  try{recognition.start();}catch(e){/* already running */}

  setStage("listening","🎤");
  statusLine.textContent="Jetzt bist du dran — sprich nach!";
  youSaid.textContent="";
  youSaid.className="you-said";

  clearTimeout(recognitionTimeout);
  recognitionTimeout=setTimeout(()=>{
    if(!gotFinal){
      try{recognition.stop();}catch(e){}
    }
  },7000);
}

if(recognition){
  recognition.onresult=(event)=>{
    let interim="";
    let final="";
    for(let i=event.resultIndex;i<event.results.length;i++){
      const transcript=event.results[i][0].transcript;
      if(event.results[i].isFinal)final+=transcript;
      else interim+=transcript;
    }
    youSaid.textContent="Du sagst: "+(final||interim);
    if(final){
      gotFinal=true;
      clearTimeout(recognitionTimeout);
      handleAnswer(final);
    }
  };

  recognition.onerror=()=>{
    clearTimeout(recognitionTimeout);
  };

  recognition.onend=()=>{
    if(!gotFinal&&recognizing){
      handleAnswer("");
    }
  };
}

/* ---------- Ablauf ---------- */
function setStage(state,icon){
  stage.dataset.state=state;
  if(icon)orbIcon.textContent=icon;
}

function addToHistory(index,text){
  if(historyEmpty)historyEmpty.remove();
  const item=document.createElement("p");
  item.className="history-item";
  item.innerHTML=`<span class="h-num">${index+1}.</span>${text}`;
  historyList.appendChild(item);
  historyList.scrollTop=historyList.scrollHeight;
}

function clearHistory(){
  historyList.innerHTML="";
  const empty=document.createElement("p");
  empty.className="history-empty";
  empty.id="history-empty";
  empty.textContent="Hier erscheinen deine richtig gesprochenen Sätze.";
  historyList.appendChild(empty);
}

function updateProgress(){
  const pct=Math.round((currentIndex/sentences.length)*100);
  progressFill.style.width=pct+"%";
  progressCount.textContent=Math.min(currentIndex,sentences.length)+" / "+sentences.length;
}

async function playCurrentAndListen(){
  if(currentIndex>=sentences.length){
    finishCourse();
    return;
  }
  recognizing=false;
  updateProgress();
  setStage("speaking","🔊");
  statusLine.textContent="Hör gut zu …";
  youSaid.textContent="";
  youSaid.className="you-said";

  await speak(sentences[currentIndex],currentRate);

  recognizing=true;
  if(recognition){
    startListening();
  }else{
    setStage("listening","⌨️");
    statusLine.textContent="Tippe den Satz unten ein und drücke Senden.";
  }
}

function handleAnswer(said){
  recognizing=false;
  if(recognition){try{recognition.stop();}catch(e){}}

  const target=sentences[currentIndex];

  if(!said||!said.trim()){
    statusLine.textContent="Ich habe nichts gehört. Hier ist der Satz noch einmal:";
    setStage("incorrect","🙉");
    setTimeout(()=>{ playCurrentAndListen(); },1200);
    return;
  }

  if(isMatch(said,target)){
    youSaid.textContent="Du: "+said;
    youSaid.className="you-said ok";
    setStage("correct","✅");
    statusLine.textContent="Sehr gut! ✨";
    addToHistory(currentIndex,target);
    currentIndex++;
    setTimeout(()=>{ playCurrentAndListen(); },1100);
  }else{
    youSaid.textContent="Du: "+said;
    youSaid.className="you-said bad";
    setStage("incorrect","🙈");
    statusLine.textContent="Nicht ganz. Bitte noch einmal.";
    speak("Nicht ganz, bitte noch einmal.",1).then(()=>{
      setTimeout(()=>{ playCurrentAndListen(); },400);
    });
  }
}

function finishCourse(){
  updateProgress();
  setStage("done","🏆");
  statusLine.textContent="";
  sentenceText.textContent="";
  youSaid.textContent="";
  controls.classList.remove("active");
  doneWrap.classList.add("active");
}

/* ---------- Buttons ---------- */
document.getElementById("start-btn").addEventListener("click",()=>{
  startWrap.style.display="none";
  exerciseWrap.style.display="block";
  controls.classList.add("active");
  playCurrentAndListen();
});

document.getElementById("repeat-btn").addEventListener("click",()=>{
  if(currentIndex<sentences.length){
    playCurrentAndListen();
  }
});

document.getElementById("next-btn").addEventListener("click",()=>{
  recognizing=false;
  if(recognition){try{recognition.stop();}catch(e){}}
  if("speechSynthesis" in window)speechSynthesis.cancel();
  clearTimeout(recognitionTimeout);
  if(currentIndex<sentences.length){
    currentIndex++;
  }
  playCurrentAndListen();
});

document.getElementById("weiter-btn").addEventListener("click",()=>{
  if(currentIndex<sentences.length){
    playCurrentAndListen();
  }
});

document.getElementById("stop-btn").addEventListener("click",()=>{
  recognizing=false;
  if(recognition){try{recognition.stop();}catch(e){}}
  if("speechSynthesis" in window)speechSynthesis.cancel();
  setStage("idle","⏸");
  statusLine.textContent="Angehalten. Drücke „Letzte Frage wiederholen“, um weiterzumachen.";
});

function restartAll(){
  recognizing=false;
  if(recognition){try{recognition.stop();}catch(e){}}
  if("speechSynthesis" in window)speechSynthesis.cancel();
  currentIndex=0;
  doneWrap.classList.remove("active");
  controls.classList.add("active");
  clearHistory();
  updateProgress();
  playCurrentAndListen();
}

document.getElementById("restart-btn").addEventListener("click",restartAll);
document.getElementById("done-restart-btn").addEventListener("click",restartAll);

const speedInput=document.getElementById("speed");
const speedVal=document.getElementById("speed-val");
speedInput.addEventListener("input",()=>{
  currentRate=parseFloat(speedInput.value);
  speedVal.textContent=currentRate.toFixed(2);
});

const textInput=document.getElementById("text-input");
const sendBtn=document.getElementById("send-btn");

function submitTyped(){
  if(currentIndex>=sentences.length)return;
  const val=textInput.value;
  textInput.value="";
  if(recognition){try{recognition.stop();}catch(e){}}
  clearTimeout(recognitionTimeout);
  handleAnswer(val);
}

sendBtn.addEventListener("click",submitTyped);
textInput.addEventListener("keydown",(e)=>{
  if(e.key==="Enter")submitTyped();
});

updateProgress();
</script>

</body>
</html>
